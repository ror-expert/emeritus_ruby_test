<h1>Editing course</h1>

<%= render "form", course: @course, url: school_course_path(@school, @course) %>

<br>

<div>
  <%= link_to "Show this course", school_course_path(@school, @course) %> |
  <%= link_to "Back to courses", school_courses_path(@school) %>
</div>
